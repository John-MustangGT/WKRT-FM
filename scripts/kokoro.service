[Unit]
Description=Kokoro TTS API Service
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/kokoro
ExecStart=/opt/kokoro/venv/bin/uvicorn server:app --host 0.0.0.0 --port 8880
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target